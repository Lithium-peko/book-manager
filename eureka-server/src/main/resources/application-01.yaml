server:
  port: 8801

spring:
  application:
    name: eureka-server

eureka:
  instance:
    hostname: eureka01

  client:
    fetch-registry: false
    service-url:
      defaultZone: http://eureka02:8802/eureka